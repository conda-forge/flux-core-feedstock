From 059563b918dbf8f4f6d117056476b066a8321e90 Mon Sep 17 00:00:00 2001
From: Jan Janssen <jan-janssen@users.noreply.github.com>
Date: Wed, 6 May 2026 09:08:55 +0200
Subject: [PATCH] Remove test for longer prefix command handling

Removed test for commands with longer prefixes from basic tests.
---
 t/t0001-basic.t | 11 -----------
 1 file changed, 11 deletions(-)

diff --git a/t/t0001-basic.t b/t/t0001-basic.t
index 4543f4fe2c33..43097e94913f 100755
--- a/t/t0001-basic.t
+++ b/t/t0001-basic.t
@@ -884,17 +884,6 @@ test_expect_success 'commands prefixed with the invalid command are favored' '
 	test_must_fail grep "mee" invalid6.out
 '
 
-test_expect_success 'commands with longer prefixes get special treatment' '
-	TEMPDIR1=$(mktemp -d) &&
-	touch $TEMPDIR1/flux-python3.12 &&
-	chmod +x $TEMPDIR1/flux-python3.12 &&
-	touch $TEMPDIR1/flux-python3.157 &&
-	chmod +x $TEMPDIR1/flux-python3.157 &&
-	test_must_fail sh -c "FLUX_EXEC_PATH=$TEMPDIR1 flux python3 > invalid7.out 2>&1" &&
-	grep "python3.12" invalid7.out &&
-	grep "python3.157" invalid7.out
-'
-
 test_expect_success 'at most 3 suggested similar commands are output' '
 	TEMPDIR=$(mktemp -d) &&
 	touch $TEMPDIR/flux-mama &&
