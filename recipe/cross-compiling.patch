From b3651dcf40d51f829499dacc6dd8d0e84b070fdb Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Jan=20Jan=C3=9Fen?= <janssen@mpie.de>
Date: Fri, 3 Oct 2025 17:15:31 +0200
Subject: [PATCH] Fix zero message queue test

---
 configure | 53 -----------------------------------------------------
 1 file changed, 53 deletions(-)

diff --git a/configure b/configure
index 3b34720..416d46d 100755
--- a/configure
+++ b/configure
@@ -18776,59 +18776,6 @@ printf "%s\n" "yes" >&6; }
 	:
 fi
 
-    old_CFLAGS=$CFLAGS
-    CFLAGS="$CFLAGS $ZMQ_CFLAGS"
-
-    old_LIBS=$LIBS
-    LIBS="$LIBS $ZMQ_LIBS"
-
-    { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking whether zeromq has CURVE support" >&5
-printf %s "checking whether zeromq has CURVE support... " >&6; }
-    if test "$cross_compiling" = yes
-then :
-  { { printf "%s\n" "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
-printf "%s\n" "$as_me: error: in \`$ac_pwd':" >&2;}
-as_fn_error $? "cannot run test program while cross compiling
-See \`config.log' for more details" "$LINENO" 5; }
-else $as_nop
-  cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-
-
-            #include <zmq.h>
-	    int main () {
-	        char pub[41];
-	        char sec[41];
-	        if (zmq_curve_keypair (pub, sec) < 0) return 1;
-	        return 0;
-	    }
-
-_ACEOF
-if ac_fn_c_try_run "$LINENO"
-then :
-  have_zmq_curve_support=yes; { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: yes" >&5
-printf "%s\n" "yes" >&6; }
-else $as_nop
-  { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: no" >&5
-printf "%s\n" "no" >&6; }
-
-fi
-rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext \
-  conftest.$ac_objext conftest.beam conftest.$ac_ext
-fi
-
-
-    CFLAGS=$old_CFLAGS
-    LIBS=$old_LIBS
-
-    if test "x$have_zmq_curve_support" != "xyes"
-then :
-  as_fn_error $? "zeromq CURVE/libsodium support is required" "$LINENO" 5
-
-fi
-
-
-
 
 
 pkg_failed=no
